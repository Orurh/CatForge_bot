services:
  nginx:
    # dev: no local TLS, no certs
    ports:
      - "80:80"
    volumes:
      - ./deploy/nginx.dev.conf:/etc/nginx/conf.d/default.conf:ro
    # override to avoid cert volume in dev
    # (compose merges volumes, so we replace list полностью)